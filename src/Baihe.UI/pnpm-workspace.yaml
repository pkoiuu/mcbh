# pnpm v11 配置文件
# 使用 hoisted 模式，避免 Windows 上的符号链接 EBUSY 问题
# 同时确保 CI 环境也使用 hoisted 模式，避免 .pnpm 隔离目录导致依赖解析失败
nodeLinker: hoisted
# 允许 esbuild 运行构建脚本（下载平台特定的二进制文件）
onlyBuiltDependencies:
  - esbuild
# 跳过运行脚本前的依赖检查
# 在 Windows 上，依赖检查会触发 registerProject，导致 EBUSY 错误
verifyDepsBeforeRun: false
